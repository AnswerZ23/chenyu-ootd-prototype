# 男装车内 OOTD 商家工作台原型 Design QA

## 基本信息

- Source visual direction: `C:\Users\rules\.codex\generated_images\019ece9d-06fd-70d0-8881-e747ac1b3ec0\ig_09a39cf410752d88016a310df623d8819ab3dee75ddf8707ce.png`
- Implementation screenshot: `C:\Users\rules\Documents\晨羽智云项目\ootd-merchant-prototype\prototype-screenshot-v3.png`
- Result screenshot: `C:\Users\rules\Documents\晨羽智云项目\ootd-merchant-prototype\result-screenshot.png`
- Comparison image: `C:\Users\rules\Documents\晨羽智云项目\ootd-merchant-prototype\design-qa-comparison.png`
- Viewport checked: 1440 x 1024
- Checked state: 视频参数配置页，预览图已确认作为下一工作流 input

## 视觉对照

- 三栏商家工作台结构已落地：左侧流程导航、中间参数配置、右侧预览和参数汇总。
- 主视觉语气已匹配：灰白底、细分割线、绿色成功态、黑色主行动按钮、紧凑运营后台密度。
- PRD v0.2 关键链路已体现在页面中：预览图作为 input、动作/风景白话输入、AI 润色结果、模型选择、视频数量。
- 使用项目已有车内帧图作为预览素材，保证原型能表达真实产线场景。

## 交互验证

- 参数页可切换动作快捷项，动作白话输入与 AI 润色结果会同步更新。
- 参数页可切换风景快捷项，风景白话输入与 AI 润色结果会同步更新。
- 模型可在开源/闭源之间切换。
- 视频数量支持加减，默认 1 条。
- 点击生成视频后进入生成中状态，并自动进入结果与下载页。
- 结果页可下载，显示“已加入下载队列”提示。
- 结果页可重抽视频，回到生成中状态。
- 任务列表可进入，并展示任务、预览图 input、AI 润色等记录；可返回生成工作台。

## 修正记录

- 已压缩参数页垂直节奏，确保 1440 x 1024 视口下“生成视频”按钮在首屏可见。
- 复测任务列表时按实际页面结构检查任务项，确认任务列表已正常渲染 3 条记录。
- 已重构“上传素材”和“预览图生成”页面：上传页改为双素材上传卡 + 参考模板条，预览页改为 output 主审图 + 原始素材辅助信息，避免横向大图裁切。
- 已将原型从步骤切换改为单页连续产线：上传素材、预览图 output、视频参数配置、生成中、结果与下载均在同一页面内展示；左侧流程改为页内定位导航。
- 已将任务列表从工作台产线中移除，改为仅通过左侧“查看任务列表”进入独立任务列表视图；返回后回到单页工作台。
- 已新增“选择爆款模板”作为上传素材前置流程。
- 已把用户提供的视频放入模板选择区，用于展示“男装车内 OOTD”模板产出形态。
- 已将上传素材区升级为多张素材上传规范：模特图片支持正脸照、全身照、侧脸照；服装图片支持正面平铺图、背面平铺图、细节特写图、上身效果图。
- 已增加“历史素材自动带入”提示，表达首次上传后刷新或关闭页面再进入会自动带入，更新后重新记录的后台逻辑。
- 已将产品名改为“晨羽 OOTD”，副标题改为“服装素材爆款复刻机”。
- 已重构模板选择区：默认选中男装车内 OOTD，去除单独“当前选择”说明块；模板视频改为小尺寸竖屏卡片，并预留 4 个未来模板位。

## 单页产线验证

- 同一页面已同时渲染 6 个核心步骤区块：选择模板、上传素材、预览图 output、视频参数配置、生成中、结果与下载。
- 左侧“视频参数配置”点击后会滚动到对应区块，而不是切换页面。
- 点击“生成视频”后在同页展示生成进度；完成后下方“结果与下载”区块激活，下载和重抽按钮可用。
- 工作台内不再渲染任务列表；点击左侧“查看任务列表”后进入独立任务页，且可返回生成工作台。
- 模板选择区已渲染模板视频；上传区已渲染 7 个素材规范卡片，并展示自动带入提示。
- 顶部产品名、副标题、模板卡数量、预留模板位、小竖屏视频尺寸已通过浏览器校验。
- 1440 x 1024 视口下无横向溢出。

## 剩余差异

- 原型不是对 ImageGen 方向图做像素级复刻，而是按所选方向实现可点击产品原型。
- 源方向图中的部分图标细节与头像资源没有逐一复刻；当前版本优先保证业务链路、高保真布局和可交互性。

## Final Result

passed
